version: '2'

services:
  ci-build:
    image: microsoft/aspnetcore-build:2
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./WetPicsTelegramBot.sln && dotnet publish ./WetPicsTelegramBot.sln -c Release -o ./obj/Docker/publish"
